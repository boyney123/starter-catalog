## Catalog Updates

This update introduces a new event `OrderRefunded` and adds relevant details to the `README.md` of the Orders Service.

### Updates:
1. **New Event:** `OrderRefunded`
   - Added to the list of published events in `README.md` for the `OrdersService`.
   - A new command in the `services/messages.js` was also added that corresponds with the `OrderRefunded` event.

### Suggested Documentation Changes:
- Update any related flow or API contract documentation according to the new event and how it interacts with existing services.