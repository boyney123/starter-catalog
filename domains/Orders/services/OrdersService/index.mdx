---
id: OrdersService
version: 0.0.3
name: Orders Service
summary: |
  Service that manages order placement, lifecycle updates, and fulfillment decisions via its HTTP API and inventory-driven events.
owners:
  - dboyne
receives:
  - id: InventoryCreated
    version: 0.0.1
    from:
      - id: 'inventory.{env}.events'
        parameters:
          env: staging
  - id: InventoryAdjusted
    version: 1.0.1
    from:
      - id: 'inventory.{env}.events'
  - id: CreateReturnLabel
  - id: PlaceOrder
  - id: GetOrder
sends:
  - id: OrderAmended
    to:
      - id: 'orders.{env}.events'
        parameters:
          env: staging
  - id: OrderCancelled
    to:
      - id: 'orders.{env}.events'
  - id: OrderConfirmed
    to:
      - id: 'orders.{env}.events'
  - id: PaymentReceived
    version: 0.0.1
    to:
      - id: 'orders.{env}.events'
  - id: OrderShipped
    version: 0.0.1
    to:
      - id: 'orders.{env}.events'
  - id: OrderDelivered
    version: 0.0.1
    to:
      - id: 'orders.{env}.events'
  - id: OrderReturned
    version: 0.0.1
    to:
      - id: 'orders.{env}.events'
  - id: AddInventory
    version: 0.0.3
    to:
      - id: 'inventory.{env}.events'
        parameters:
          env: staging
repository:
  language: JavaScript
  url: 'https://github.com/event-catalog/pretend-shipping-service'
schemaPath: openapi.yml
specifications:
  asyncapiPath: order-service-asyncapi.yaml
  openapiPath: openapi.yml
---

import Footer from '@catalog/components/footer.astro';

## Overview

The Orders Service is responsible for managing customer orders within the system. It handles order creation, updating, status tracking, and interactions with other services such as Inventory, Payment, and Notification services to ensure smooth order processing and fulfillment.

## HTTP API
The REST API for this service is described in the OpenAPI specification linked in this page. It exposes lightweight endpoints for service health and the full order lifecycle:

- `GET /health` — returns a simple health response that includes the service name and a status flag.
- `POST /orders` — accepts the `PlaceOrderCommand`, schedules the order for processing, and returns the created order along with the events that were published as a result.
- `GET /orders` — lists all orders currently tracked by the service.
- `GET /orders/{orderId}` — fetches a single order by identifier.
- `DELETE /orders/{orderId}` — requests order cancellation and captures an optional reason for the cancellation.
- `POST /orders/{orderId}/refund` — records a refund request for an existing order including the amount and motivation.

## Inventory readiness
The Orders Service listens for `InventoryCreated` events on the inventory events channel. When a product that previously lacked stock becomes available again, the service re-evaluates orders marked as `awaiting-stock` for that SKU, transitions them to `confirmed`, and republishes an `OrderConfirmed` event so downstream consumers can continue processing.

## Payment and fulfillment events
Payment capture, shipping, delivery, and return milestones are also emitted on the orders events channel. The service publishes `PaymentReceived` when payments clear, then `OrderShipped`, `OrderDelivered`, and `OrderReturned` as respective fulfillment steps complete so downstream services can trigger notifications, inventory reconciliation, or refunds.

<Tiles>
  <Tile
    icon="DocumentIcon"
    href={`/docs/services/${frontmatter.id}/${frontmatter.version}/changelog`}
    title="View the changelog"
    description="Want to know the history of this service? View the change logs"
  />
  <Tile
    icon="UserGroupIcon"
    href="/docs/teams/full-stack"
    title="Contact the team"
    description="Any questions? Feel free to contact the owners"
  />
  <Tile
    icon="BoltIcon"
    href={`/visualiser/services/${frontmatter.id}/${frontmatter.version}`}
    title={`Sends ${frontmatter.sends.length} messages`}
    description="This service sends messages to downstream consumers"
  />
  <Tile
    icon="BoltIcon"
    href={`/visualiser/services/${frontmatter.id}/${frontmatter.version}`}
    title={`Receives ${frontmatter.receives.length} messages`}
    description="This service receives messages from other services"
  />
</Tiles>

## Architecture diagram

<NodeGraph />

<Footer />
