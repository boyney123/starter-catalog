{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OrderRefundedEvent",
  "type": "object",
  "properties": {
    "orderId": {
      "type": "string",
      "description": "The unique identifier of the refunded order."
    },
    "userId": {
      "type": "string",
      "description": "The unique identifier of the user who placed the order."
    },
    "orderItems": {
      "type": "array",
      "description": "A list of items included in the refunded order, each containing product details and quantities.",
      "items": {
        "type": "object",
        "properties": {
          "productId": {
            "type": "string",
            "description": "The unique identifier of the product."
          },
          "productName": {
            "type": "string",
            "description": "The name of the product."
          },
          "quantity": {
            "type": "integer",
            "description": "The quantity of the product ordered."
          },
          "unitPrice": {
            "type": "number",
            "description": "The price per unit of the product."
          },
          "totalPrice": {
            "type": "number",
            "description": "The total price for this order item (quantity * unitPrice)."
          }
        },
        "required": ["productId", "productName", "quantity", "unitPrice", "totalPrice"]
      }
    },
    "orderStatus": {
      "type": "string",
      "enum": ["refunded"],
      "description": "The status of the order after the refund. Always 'refunded' for this event."
    },
    "totalAmount": {
      "type": "number",
      "description": "The total amount of the original order."
    },
    "refundAmount": {
      "type": "number",
      "description": "The amount refunded to the customer."
    },
    "refundReason": {
      "type": "string",
      "description": "The reason for the refund."
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "The date and time when the refund was issued."
    }
  },
  "required": ["orderId", "userId", "orderItems", "orderStatus", "totalAmount", "refundAmount", "refundReason", "timestamp"],
  "additionalProperties": false
}
